=====================================================================
 ACID TEST TRANSCRIPT
 Generated by run_acid_tests.sh against MySQL 8.0.46

 Every statement below runs against the UNMODIFIED schema from
 01_create_tables.sql. Nothing is dropped, altered or weakened first.

 ERROR 3819 (CHECK), 1451/1452 (foreign key) and 1644 (our own SIGNAL)
 are EXPECTED RESULTS, not failures. In the -FIX tests an ERROR 1644 is
 a PASS: a procedure from 07_transactions.sql refusing the statement.

 Verdicts are computed by COUNT(*) against real table contents, never
 asserted by a literal. Scroll to the VERDICT TABLE at the bottom.
=====================================================================

status	n
procedures created	4
mysql: [Warning] Using a password on the command line interface can be insecure.
--------------
DROP TABLE IF EXISTS acid_results
--------------

--------------
CREATE TABLE acid_results (
    seq       INT           NOT NULL AUTO_INCREMENT
  , test      VARCHAR(12)   NOT NULL
  , property  CHAR(1)       NOT NULL      -- A, C, or P for preflight
  , verdict   VARCHAR(8)    NOT NULL      -- GAP / OK / FAIL
  , evidence  VARCHAR(120)                -- the number the verdict came from
  , detail    VARCHAR(255)
  , PRIMARY KEY (seq)
)
--------------

--------------
-- =====================================================================
--------------

--------------
-- P0 -- PREFLIGHT. Everything below depends on these being true.
--------------

--------------
-- =====================================================================
--------------

--------------
SELECT VERSION()                          AS mysql_version
     , @@transaction_isolation            AS isolation_level
     , @@autocommit                       AS autocommit
     , @@innodb_flush_log_at_trx_commit   AS flush_log_at_commit
     , @@innodb_lock_wait_timeout         AS lock_wait_timeout
--------------

+---------------+-----------------+------------+---------------------+-------------------+
| mysql_version | isolation_level | autocommit | flush_log_at_commit | lock_wait_timeout |
+---------------+-----------------+------------+---------------------+-------------------+
| 8.0.46        | REPEATABLE-READ |          1 |                   1 |                50 |
+---------------+-----------------+------------+---------------------+-------------------+
--------------
-- On MyISAM the rollbacks below would silently do nothing and every A test
--------------

--------------
-- would "pass" for the wrong reason, so this is checked first.
--------------

--------------
SELECT table_name, engine
FROM information_schema.TABLES
WHERE table_schema = DATABASE() AND engine <> 'InnoDB'
--------------

--------------
SELECT COUNT(*) INTO @non_innodb
FROM information_schema.TABLES
WHERE table_schema = DATABASE() AND engine <> 'InnoDB'
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('P0-engine', 'P', IF(@non_innodb = 0, 'OK', 'FAIL'),
 CONCAT('non-InnoDB tables: ', @non_innodb),
 'InnoDB is required for START TRANSACTION, ROLLBACK, row locks and FKs to be real')
--------------

--------------
SELECT COUNT(*) INTO @n_checks
FROM information_schema.CHECK_CONSTRAINTS
WHERE CONSTRAINT_SCHEMA = DATABASE()
--------------

--------------
-- Declared is not the same as enforced (pre-8.0.16 parses CHECK and ignores
--------------

--------------
-- it). Probe it: this insert must fail.
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Transactions (org_id, tournament_id, type, category, amount, date)
VALUES (1, 1, 'expense', 'acid probe', -1.00, CURRENT_DATE)
--------------

ERROR 3819 (HY000) at line 47: Check constraint 'transactions_chk_1' is violated.
--------------
SELECT COUNT(*) INTO @p0 FROM Transactions WHERE category = 'acid probe'
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('P0-check', 'P', IF(@p0 = 0, 'OK', 'FAIL'),
 CONCAT(@n_checks, ' CHECK constraints declared; negative amount accepted: ', @p0),
 'CHECK constraints must be enforced, not merely parsed')
--------------

--------------
-- #####################################################################
--------------

--------------
-- #  A T O M I C I T Y
--------------

--------------
-- #####################################################################
--------------

--------------
-- =====================================================================
--------------

--------------
-- A1 -- prize payout that half-commits
--------------

--------------
--
--------------

--------------
-- The naive version: two statements, no wrapper. autocommit is ON by default,
--------------

--------------
-- so statement 1 is already permanent when statement 2 fails. Result is a
--------------

--------------
-- payout with no matching expense -- the books no longer balance.
--------------

--------------
--
--------------

--------------
-- Deliberately a STAFF payment, not a team payout. Atomicity cannot be fixed
--------------

--------------
-- by any constraint, so this test must stay demonstrable after
--------------

--------------
-- 10_acid_fixes.sql is applied. A team payout would be refused outright by the
--------------

--------------
-- new prize-pool trigger and A1 would report OK for the wrong reason -- the
--------------

--------------
-- statement never ran, rather than the pair being atomic. Staff payments are
--------------

--------------
-- not pool-checked, so the atomicity question stays visible either way.
--------------

--------------
-- =====================================================================
--------------

--------------
INSERT INTO Payments
    (payee_type, staff_user_id, team_id, tournament_id, amount, status, payment_date)
VALUES ('staff', 12, NULL, 2, 750.00, 'paid', '2026-04-14')
--------------

--------------
-- Fails: CHECK (amount >= 0) on Transactions.
--------------

--------------
INSERT INTO Transactions (org_id, tournament_id, type, category, amount, date)
VALUES (1, 2, 'expense', 'prize payout', -750.00, '2026-04-14')
--------------

ERROR 3819 (HY000) at line 81: Check constraint 'transactions_chk_1' is violated.
--------------
SELECT COUNT(*) INTO @a1
FROM Payments WHERE tournament_id = 2 AND staff_user_id = 12 AND amount = 750.00
--------------

--------------
DELETE FROM Payments WHERE tournament_id = 2 AND staff_user_id = 12 AND amount = 750.00
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A1', 'A', IF(@a1 > 0, 'GAP', 'OK'),
 CONCAT('orphan payments left behind: ', @a1),
 'payout committed with no matching expense; the two statements are not atomic')
--------------

--------------
-- A1-FIX -- the same two statements inside a transaction.
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Payments
    (payee_type, staff_user_id, team_id, tournament_id, amount, status, payment_date)
VALUES ('staff', 12, NULL, 2, 750.00, 'paid', '2026-04-14')
--------------

--------------
INSERT INTO Transactions (org_id, tournament_id, type, category, amount, date)
VALUES (1, 2, 'expense', 'prize payout', -750.00, '2026-04-14')
--------------

ERROR 3819 (HY000) at line 100: Check constraint 'transactions_chk_1' is violated.
--------------
ROLLBACK
--------------

--------------
SELECT COUNT(*) INTO @a1f
FROM Payments WHERE tournament_id = 2 AND staff_user_id = 12 AND amount = 750.00
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A1-FIX', 'A', IF(@a1f = 0, 'OK', 'FAIL'),
 CONCAT('residue after ROLLBACK: ', @a1f),
 'wrapping the pair in a transaction makes the payout all-or-nothing')
--------------

--------------
-- =====================================================================
--------------

--------------
-- A2 -- half-registered competitor
--------------

--------------
--
--------------

--------------
-- Players -> Competitor -> Registration, naive again. The third statement
--------------

--------------
-- fails on an unknown tournament_id, leaving a Competitor that entered nothing.
--------------

--------------
-- =====================================================================
--------------

--------------
INSERT INTO Players (ign) VALUES ('acid-ghost#404')
--------------

--------------
SET @gp := LAST_INSERT_ID()
--------------

--------------
INSERT INTO Competitor (competitor_type, team_id, player_id)
VALUES ('player', NULL, @gp)
--------------

--------------
SET @gc := LAST_INSERT_ID()
--------------

--------------
-- Fails: foreign key, tournament 999 does not exist.
--------------

--------------
INSERT INTO Registration (competitor_id, tournament_id, registration_date, seed)
VALUES (@gc, 999, CURRENT_DATE, 99)
--------------

ERROR 1452 (23000) at line 127: Cannot add or update a child row: a foreign key constraint fails (`design_project_370`.`registration`, CONSTRAINT `registration_ibfk_2` FOREIGN KEY (`tournament_id`) REFERENCES `tournament` (`tournament_id`))
--------------
SELECT COUNT(*) INTO @a2
FROM Competitor c
LEFT JOIN Registration r ON r.competitor_id = c.competitor_id
WHERE c.competitor_id = @gc AND r.competitor_id IS NULL
--------------

--------------
DELETE FROM Competitor WHERE competitor_id = @gc
--------------

--------------
DELETE FROM Players    WHERE player_id     = @gp
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A2', 'A', IF(@a2 > 0, 'GAP', 'OK'),
 CONCAT('competitors registered for nothing: ', @a2),
 'a Players and Competitor row survived a failed Registration insert')
--------------

--------------
-- =====================================================================
--------------

--------------
-- A5 -- DDL forces an implicit commit, so ROLLBACK cannot undo the data
--------------

--------------
--
--------------

--------------
-- This is our Sprint 3 provisioning flow: create the org, its admin user, its
--------------

--------------
-- membership, then the MySQL account that 04_views.sql matches on
--------------

--------------
-- CURRENT_USER(). CREATE USER is DDL. The three data rows survive the
--------------

--------------
-- ROLLBACK, so account provisioning is NOT atomic with the data it maps to.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Organization (org_name, contact_email, created_date, region)
VALUES ('ACID Probe Events', 'org@acid.test', CURRENT_DATE, 'EU')
--------------

--------------
SET @porg := LAST_INSERT_ID()
--------------

--------------
INSERT INTO Users (full_name, email, password, db_username)
VALUES ('ACID Probe Admin', 'admin@acid.test', 'hash', 'acid_probe')
--------------

--------------
SET @puser := LAST_INSERT_ID()
--------------

--------------
INSERT INTO Membership (user_id, org_id, role, joined_date)
VALUES (@puser, @porg, 'admin', CURRENT_DATE)
--------------

--------------
CREATE USER IF NOT EXISTS 'acid_probe'@'localhost' IDENTIFIED BY 'Csc370demo!'
--------------

--------------
ROLLBACK
--------------

--------------
SELECT COUNT(*) INTO @a5 FROM Organization WHERE org_id = @porg
--------------

--------------
DROP USER IF EXISTS 'acid_probe'@'localhost'
--------------

--------------
DELETE FROM Membership   WHERE org_id  = @porg
--------------

--------------
DELETE FROM Users        WHERE user_id = @puser
--------------

--------------
DELETE FROM Organization WHERE org_id  = @porg
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A5', 'A', IF(@a5 > 0, 'GAP', 'OK'),
 CONCAT('org rows surviving ROLLBACK: ', @a5),
 'CREATE USER is DDL and forces an implicit commit; Sprint 3 account provisioning is not atomic with its data')
--------------

--------------
-- =====================================================================
--------------

--------------
-- A6 -- hand-rolled cascade aborts partway
--------------

--------------
--
--------------

--------------
-- No FK in 01_create_tables.sql declares ON DELETE, so every one is implicit
--------------

--------------
-- RESTRICT. Deleting a tournament means deleting its children by hand, and the
--------------

--------------
-- parent DELETE still fails on Contracts / Registration / Payments /
--------------

--------------
-- Transactions -- after the bracket is already gone. Shown inside a transaction
--------------

--------------
-- so the data comes back; with autocommit ON the DELETEs before the failure
--------------

--------------
-- would be permanent.
--------------

--------------
--
--------------

--------------
-- Tournament 2 owns matches 8, 9, 10. FOUR tables reference `Match`, and all
--------------

--------------
-- four must be cleared before `Match` itself will delete. The first version of
--------------

--------------
-- this test cleared only MatchParticipant and failed one step earlier -- which
--------------

--------------
-- is itself the point: nobody hand-writes this chain correctly the first time.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
DELETE FROM MatchParticipant WHERE match_id IN (8, 9, 10)
--------------

--------------
DELETE FROM PlayerMatchStats WHERE match_id IN (8, 9, 10)
--------------

--------------
DELETE FROM StaffMatches     WHERE match_id IN (8, 9, 10)
--------------

--------------
DELETE FROM CreatorMatches   WHERE match_id IN (8, 9, 10)
--------------

--------------
DELETE FROM `Match`          WHERE tournament_id = 2
--------------

--------------
DELETE FROM Tournament       WHERE tournament_id = 2   -- fails: ERROR 1451
--------------

ERROR 1451 (23000) at line 203: Cannot delete or update a parent row: a foreign key constraint fails (`design_project_370`.`contracts`, CONSTRAINT `contracts_ibfk_2` FOREIGN KEY (`tournament_id`) REFERENCES `tournament` (`tournament_id`))
--------------
SELECT COUNT(*) INTO @a6m FROM `Match`    WHERE tournament_id = 2
--------------

--------------
SELECT COUNT(*) INTO @a6t FROM Tournament WHERE tournament_id = 2
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A6', 'A', IF(@a6m = 0 AND @a6t = 1, 'GAP', 'OK'),
 CONCAT('matches left: ', @a6m, ', tournament rows left: ', @a6t),
 'bracket destroyed while the tournament survived; a hand-rolled cascade is not atomic even after 10_acid_fixes.sql, because each DELETE is its own statement')
--------------

--------------
-- A6-FIX -- the same intent as ONE statement.
--------------

--------------
--
--------------

--------------
-- A single DELETE is atomic by itself: it either applies completely or not at
--------------

--------------
-- all, whatever it cascades into. Before 10_acid_fixes.sql this fails
--------------

--------------
-- immediately on the Match foreign key; after it, the delete cascades through
--------------

--------------
-- Match and its four child tables and THEN fails on Registration / Payments /
--------------

--------------
-- Transactions / Contracts -- and the entire statement, cascades included,
--------------

--------------
-- rolls back. Either way nothing is destroyed, which is the point: stop
--------------

--------------
-- hand-rolling the cascade.
--------------

--------------
START TRANSACTION
--------------

--------------
DELETE FROM Tournament WHERE tournament_id = 2   -- fails: ERROR 1451
--------------

ERROR 1451 (23000) at line 225: Cannot delete or update a parent row: a foreign key constraint fails (`design_project_370`.`contracts`, CONSTRAINT `contracts_ibfk_2` FOREIGN KEY (`tournament_id`) REFERENCES `tournament` (`tournament_id`))
--------------
SELECT COUNT(*) INTO @a6fm FROM `Match`          WHERE tournament_id = 2
--------------

--------------
SELECT COUNT(*) INTO @a6fp FROM MatchParticipant WHERE match_id IN (8, 9, 10)
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('A6-FIX', 'A', IF(@a6fm = 3 AND @a6fp = 6, 'OK', 'FAIL'),
 CONCAT('matches intact: ', @a6fm, ' of 3, participants intact: ', @a6fp, ' of 6'),
 'one statement is atomic on its own; the failed parent delete took its cascades down with it')
--------------

--------------
-- #####################################################################
--------------

--------------
-- #  C O N S I S T E N C Y
--------------

--------------
-- #
--------------

--------------
-- #  Each of these is ONE statement that succeeds and should not. Every one
--------------

--------------
-- #  corresponds to an "-- APP/TRIGGER" note or a Sprint 1 constraint that was
--------------

--------------
-- #  never implemented.
--------------

--------------
-- #####################################################################
--------------

--------------
-- =====================================================================
--------------

--------------
-- C1 -- cross-game registration
--------------

--------------
-- Competitor 1 is a Valorant team (game 1). Tournament 3 is Teamfight Tactics
--------------

--------------
-- (game 2). See the APP/TRIGGER note on Registration.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Registration (competitor_id, tournament_id, registration_date, seed)
VALUES (1, 3, '2026-04-25', 9)
--------------

--------------
SELECT COUNT(*) INTO @c1 FROM Registration WHERE competitor_id = 1 AND tournament_id = 3
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C1', 'C', IF(@c1 > 0, 'GAP', 'OK'),
 CONCAT('cross-game registrations accepted: ', @c1),
 'a Valorant team was entered into the TFT event; v_registration_violations only detects this after commit')
--------------

--------------
-- C1-FIX -- the same registration through sp_register_team. ERROR 1644 = pass.
--------------

--------------
CALL sp_register_team(1, 3, 9)
--------------

ERROR 1644 (45000) at line 262: team plays a different game than this tournament
--------------
SELECT COUNT(*) INTO @c1f FROM Registration WHERE competitor_id = 1 AND tournament_id = 3
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C1-FIX', 'C', IF(@c1f = 0, 'OK', 'FAIL'),
 CONCAT('rows created by the procedure: ', @c1f),
 'sp_register_team compares Teams.game_id against Tournament.game_id and refuses')
--------------

--------------
-- C1-HAPPY -- a valid registration must still go through, or the fix is
--------------

--------------
-- useless. Team 5 is Valorant, tournament 2 is Valorant, seed 5 is free.
--------------

--------------
CALL sp_register_team(5, 2, 5)
--------------

--------------
SELECT COUNT(*) INTO @c1h FROM Registration WHERE competitor_id = 5 AND tournament_id = 2
--------------

--------------
DELETE FROM Registration WHERE competitor_id = 5 AND tournament_id = 2
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C1-HAPPY', 'C', IF(@c1h = 1, 'OK', 'FAIL'),
 CONCAT('valid registrations accepted: ', @c1h),
 'the fix must not reject legal work')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C2 -- a competitor who never entered appears in a match
--------------

--------------
-- Competitor 9 is a solo TFT player. Match 7 is the Valorant Creator Cup
--------------

--------------
-- final. Nothing links MatchParticipant to Registration.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO MatchParticipant (match_id, competitor_id, placement, points)
VALUES (7, 9, 3, 0)
--------------

--------------
SELECT COUNT(*) INTO @c2 FROM MatchParticipant WHERE match_id = 7 AND competitor_id = 9
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C2', 'C', IF(@c2 > 0, 'GAP', 'OK'),
 CONCAT('unregistered participants accepted: ', @c2),
 'a solo TFT player was placed in a Valorant final')
--------------

--------------
-- C2-FIX -- ERROR 1644 = pass.
--------------

--------------
CALL sp_record_placement(7, 9, 3, 0)
--------------

ERROR 1644 (45000) at line 301: competitor is not registered for this match's tournament
--------------
SELECT COUNT(*) INTO @c2f FROM MatchParticipant WHERE match_id = 7 AND competitor_id = 9
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C2-FIX', 'C', IF(@c2f = 0, 'OK', 'FAIL'),
 CONCAT('rows created by the procedure: ', @c2f),
 'sp_record_placement requires a matching Registration row')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C3 -- two winners of the same match
--------------

--------------
-- Match 7 already has competitor 1 at placement 1. There is no
--------------

--------------
-- UNIQUE (match_id, placement).
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO MatchParticipant (match_id, competitor_id, placement, points)
VALUES (7, 5, 1, 1)
--------------

--------------
SELECT COUNT(*) INTO @c3 FROM MatchParticipant WHERE match_id = 7 AND placement = 1
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C3', 'C', IF(@c3 > 1, 'GAP', 'OK'),
 CONCAT('competitors at placement 1 in match 7: ', @c3),
 'two first places in one match; add UNIQUE (match_id, placement)')
--------------

--------------
-- C3-FIX -- competitor 5 IS registered for tournament 1, so this reaches the
--------------

--------------
-- placement check rather than the registration check. ERROR 1644 = pass.
--------------

--------------
CALL sp_record_placement(7, 5, 1, 1)
--------------

ERROR 1644 (45000) at line 329: that placement is already taken in this match
--------------
SELECT COUNT(*) INTO @c3f FROM MatchParticipant WHERE match_id = 7 AND placement = 1
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C3-FIX', 'C', IF(@c3f = 1, 'OK', 'FAIL'),
 CONCAT('competitors at placement 1 in match 7: ', @c3f),
 'sp_record_placement rejects a placement already taken in that match')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C4 -- duplicate seed
--------------

--------------
-- Tournament 2 already has seed 1 (competitor 1). There is no
--------------

--------------
-- UNIQUE (tournament_id, seed).
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Registration (competitor_id, tournament_id, registration_date, seed)
VALUES (5, 2, '2026-04-02', 1)
--------------

--------------
SELECT COUNT(*) INTO @c4 FROM Registration WHERE tournament_id = 2 AND seed = 1
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C4', 'C', IF(@c4 > 1, 'GAP', 'OK'),
 CONCAT('competitors on seed 1 in tournament 2: ', @c4),
 'duplicate seed accepted; add UNIQUE (tournament_id, seed)')
--------------

--------------
-- C4-FIX -- ERROR 1644 = pass.
--------------

--------------
CALL sp_register_team(5, 2, 1)
--------------

ERROR 1644 (45000) at line 356: seed already taken in this tournament
--------------
SELECT COUNT(*) INTO @c4f FROM Registration WHERE tournament_id = 2 AND seed = 1
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C4-FIX', 'C', IF(@c4f = 1, 'OK', 'FAIL'),
 CONCAT('competitors on seed 1 in tournament 2: ', @c4f),
 'sp_register_team refuses a seed that is already taken')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C5 -- one player active on two teams at once
--------------

--------------
-- Player 1 already has a Roster row on team 1 with leave_date IS NULL.
--------------

--------------
-- See the APP/TRIGGER note on Roster.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Roster (player_id, team_id, join_date, leave_date, salary, jersey_number)
VALUES (1, 2, '2026-01-01', NULL, 1300.00, 9)
--------------

--------------
SELECT COUNT(*) INTO @c5 FROM Roster WHERE player_id = 1 AND leave_date IS NULL
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C5', 'C', IF(@c5 > 1, 'GAP', 'OK'),
 CONCAT('active roster rows for player 1: ', @c5),
 'player active on two teams simultaneously; needs a trigger, no CHECK can span rows')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C6 -- match scheduled outside its tournament's date range
--------------

--------------
-- Tournament 1 ran 2026-03-14 to 2026-03-16. This was a Sprint 1 constraint
--------------

--------------
-- that never reached the schema.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO `Match` (tournament_id, scheduled_time, final_score)
VALUES (1, '2027-11-30 09:00:00', '13-0')
--------------

--------------
SELECT COUNT(*) INTO @c6
FROM `Match` m JOIN Tournament t ON t.tournament_id = m.tournament_id
WHERE m.tournament_id = 1
  AND (DATE(m.scheduled_time) < t.start_date OR DATE(m.scheduled_time) > t.end_date)
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C6', 'C', IF(@c6 > 0, 'GAP', 'OK'),
 CONCAT('matches outside their tournament window: ', @c6),
 'match accepted 20 months after the tournament ended')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C7 -- staff assigned to a tournament run by an org they do not belong to
--------------

--------------
-- User 1 is a player captain with no Membership row at all.
--------------

--------------
-- See the APP/TRIGGER note on StaffAssignments.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO StaffAssignments (user_id, tournament_id, staff_role, pay_amount)
VALUES (1, 1, 'observer', 200.00)
--------------

--------------
SELECT COUNT(*) INTO @c7
FROM StaffAssignments sa
JOIN Tournament t ON t.tournament_id = sa.tournament_id
LEFT JOIN Membership mb
       ON mb.user_id = sa.user_id AND mb.org_id = t.org_id AND mb.left_date IS NULL
WHERE sa.user_id = 1 AND mb.user_id IS NULL
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C7', 'C', IF(@c7 > 0, 'GAP', 'OK'),
 CONCAT('paid staff who are not org members: ', @c7),
 'a player captain was accepted as paid tournament staff')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C8 -- one person in two places at once, four ways
--------------

--------------
-- Match 7 is at 2026-03-16 15:00:00. Insert a second match in the same slot,
--------------

--------------
-- then double-book the caster, the creator and a player.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO `Match` (tournament_id, scheduled_time, final_score)
VALUES (1, '2026-03-16 15:00:00', '13-2')
--------------

--------------
SET @m := LAST_INSERT_ID()
--------------

--------------
INSERT INTO StaffMatches     (user_id, match_id, role) VALUES (10, @m, 'caster')
--------------

--------------
INSERT INTO CreatorMatches   (creator_id, match_id)    VALUES (1, @m)
--------------

--------------
INSERT INTO PlayerMatchStats (player_id, match_id, kills, deaths, assists, score)
VALUES (1, @m, 30, 2, 4, 400)
--------------

--------------
SELECT COUNT(*) INTO @c8s FROM StaffMatches     WHERE match_id IN (7, @m) AND user_id    = 10
--------------

--------------
SELECT COUNT(*) INTO @c8c FROM CreatorMatches   WHERE match_id IN (7, @m) AND creator_id = 1
--------------

--------------
SELECT COUNT(*) INTO @c8p FROM PlayerMatchStats WHERE match_id IN (7, @m) AND player_id  = 1
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C8', 'C', IF(@c8s > 1 AND @c8c > 1 AND @c8p > 1, 'GAP', 'OK'),
 CONCAT('at 2026-03-16 15:00 -- caster slots: ', @c8s, ', creator slots: ', @c8c, ', player slots: ', @c8p),
 'caster, creator and player all double-booked at the same instant')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C9 -- payout overdraws the prize pool
--------------

--------------
-- Tournament 1: prize_pool 5000, team payouts already total exactly 5000
--------------

--------------
-- (3000 + 1500 + 500). Any further team payment overdraws it.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Payments
    (payee_type, staff_user_id, team_id, tournament_id, amount, status, payment_date)
VALUES ('team', NULL, 3, 1, 2500.00, 'paid', '2026-03-19')
--------------

--------------
SELECT COALESCE(SUM(amount), 0) INTO @c9paid
FROM Payments WHERE tournament_id = 1 AND payee_type = 'team'
--------------

--------------
SELECT prize_pool INTO @c9pool FROM Tournament WHERE tournament_id = 1
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C9', 'C', IF(@c9paid > @c9pool, 'GAP', 'OK'),
 CONCAT('pool ', @c9pool, ' vs team payouts ', @c9paid),
 'total payouts exceeded the prize pool with no constraint objecting')
--------------

--------------
-- C9-FIX -- ERROR 1644 = pass.
--------------

--------------
CALL sp_pay_team_prize(3, 1, 2500.00)
--------------

ERROR 1644 (45000) at line 471: payout would exceed the tournament prize pool
--------------
SELECT COUNT(*) INTO @c9f
FROM Payments WHERE tournament_id = 1 AND team_id = 3 AND amount = 2500.00
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C9-FIX', 'C', IF(@c9f = 0, 'OK', 'FAIL'),
 CONCAT('overdrawing payments created: ', @c9f),
 'sp_pay_team_prize checks headroom under FOR UPDATE and refuses')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C10 -- paying a team that never entered
--------------

--------------
-- Team 5 (MENAces) is registered for tournament 1 only.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Payments
    (payee_type, staff_user_id, team_id, tournament_id, amount, status)
VALUES ('team', NULL, 5, 2, 400.00, 'pending')
--------------

--------------
SELECT COUNT(*) INTO @c10
FROM Payments p
LEFT JOIN Competitor c   ON c.team_id        = p.team_id
LEFT JOIN Registration r ON r.competitor_id  = c.competitor_id
                        AND r.tournament_id  = p.tournament_id
WHERE p.team_id = 5 AND p.tournament_id = 2 AND r.competitor_id IS NULL
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C10', 'C', IF(@c10 > 0, 'GAP', 'OK'),
 CONCAT('payments to unregistered teams: ', @c10),
 'prize money owed to a team that never entered that tournament')
--------------

--------------
-- C10-FIX -- ERROR 1644 = pass.
--------------

--------------
CALL sp_pay_team_prize(5, 2, 400.00)
--------------

ERROR 1644 (45000) at line 504: team is not registered for this tournament
--------------
SELECT COUNT(*) INTO @c10f
FROM Payments WHERE tournament_id = 2 AND team_id = 5 AND amount = 400.00
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C10-FIX', 'C', IF(@c10f = 0, 'OK', 'FAIL'),
 CONCAT('payments created: ', @c10f),
 'sp_pay_team_prize requires the team to be registered')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C11 -- cross-org attribution, which leaks through the Sprint 3 views
--------------

--------------
--
--------------

--------------
-- Nothing requires Transactions.org_id = Tournament.org_id. v_org_financials
--------------

--------------
-- filters by org_id, so a second org can book a line item onto another org's
--------------

--------------
-- tournament and then see it in its own financial report. A consistency gap
--------------

--------------
-- that punches a hole in the row-level security we claimed in Sprint 3.
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Organization (org_name, contact_email, created_date, region)
VALUES ('Rival Events', 'staff@rival.gg', CURRENT_DATE, 'EU')
--------------

--------------
SET @rival := LAST_INSERT_ID()
--------------

--------------
INSERT INTO Transactions (org_id, tournament_id, type, category, amount, date)
VALUES (@rival, 1, 'revenue', 'sponsorship', 99999.00, CURRENT_DATE)
--------------

--------------
INSERT INTO Contracts
    (org_id, tournament_id, party_type, sponsor_id, creator_id, start_date, end_date, total_value)
VALUES (@rival, 1, 'sponsor', 1, NULL, '2026-01-01', '2026-03-16', 1.00)
--------------

--------------
SELECT COUNT(*) INTO @c11tr
FROM Transactions tr JOIN Tournament t ON t.tournament_id = tr.tournament_id
WHERE tr.org_id <> t.org_id
--------------

--------------
SELECT COUNT(*) INTO @c11co
FROM Contracts co JOIN Tournament t ON t.tournament_id = co.tournament_id
WHERE co.org_id <> t.org_id
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C11', 'C', IF(@c11tr > 0 OR @c11co > 0, 'GAP', 'OK'),
 CONCAT('mismatched transactions: ', @c11tr, ', mismatched contracts: ', @c11co),
 'org_id need not match the tournament owner, so v_org_financials can show another org rows')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C12 -- NULL slips past a CHECK and past a UNIQUE
--------------

--------------
--
--------------

--------------
-- In SQL a CHECK that evaluates to UNKNOWN is accepted. Three constraints that
--------------

--------------
-- look stronger than they are:
--------------

--------------
--   Roster      CHECK (join_date <= leave_date)
--------------

--------------
--   Tournament  CHECK (start_date <= end_date)
--------------

--------------
--   Roster      UNIQUE (team_id, jersey_number)
--------------

--------------
-- =====================================================================
--------------

--------------
START TRANSACTION
--------------

--------------
INSERT INTO Roster (player_id, team_id, join_date, leave_date, jersey_number)
VALUES (2, 3, '2026-06-01', NULL, 20)
--------------

--------------
INSERT INTO Tournament (org_id, game_id, name, start_date, end_date, prize_pool)
VALUES (1, 1, 'ACID NULL-date Cup', '2026-12-01', NULL, 100.00)
--------------

--------------
INSERT INTO Roster (player_id, team_id, jersey_number) VALUES (3, 3, NULL)
--------------

--------------
INSERT INTO Roster (player_id, team_id, jersey_number) VALUES (4, 3, NULL)
--------------

--------------
SELECT COUNT(*) INTO @c12t FROM Tournament WHERE name = 'ACID NULL-date Cup'
--------------

--------------
SELECT COUNT(*) INTO @c12j FROM Roster WHERE team_id = 3 AND jersey_number IS NULL
--------------

--------------
ROLLBACK
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C12', 'C', IF(@c12t > 0 AND @c12j > 1, 'GAP', 'OK'),
 CONCAT('NULL end_date tournaments: ', @c12t, ', duplicate NULL jerseys on team 3: ', @c12j),
 'NULL evaluates a CHECK to UNKNOWN (accepted) and does not collide in a UNIQUE')
--------------

--------------
-- =====================================================================
--------------

--------------
-- C13 -- an inconsistency already committed in our own data
--------------

--------------
--
--------------

--------------
-- Nothing was inserted for this test. 02_insert_data.sql books 1200.00 of
--------------

--------------
-- staff expense for tournament 1 while StaffAssignments and Payments only
--------------

--------------
-- account for 800.00. Nothing in the schema reconciles the two.
--------------

--------------
-- =====================================================================
--------------

--------------
SELECT COALESCE(SUM(amount), 0) INTO @c13booked
FROM Transactions WHERE tournament_id = 1 AND category = 'staff'
--------------

--------------
SELECT COALESCE(SUM(amount), 0) INTO @c13paid
FROM Payments WHERE tournament_id = 1 AND payee_type = 'staff'
--------------

--------------
SELECT COALESCE(SUM(pay_amount), 0) INTO @c13agreed
FROM StaffAssignments WHERE tournament_id = 1
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('C13', 'C', IF(@c13booked <> @c13paid, 'GAP', 'OK'),
 CONCAT('booked ', @c13booked, ' vs paid ', @c13paid, ' vs agreed ', @c13agreed),
 'already inconsistent in committed data -- nothing was injected for this test')
--------------

--------------
-- =====================================================================
--------------

--------------
-- RESIDUE -- must be all zeros. The tests above are designed to leave the
--------------

--------------
-- database exactly as 02_insert_data.sql left it.
--------------

--------------
-- =====================================================================
--------------

--------------
SELECT (SELECT COUNT(*) FROM Players      WHERE ign LIKE 'acid-%')
     + (SELECT COUNT(*) FROM Organization WHERE org_name LIKE 'ACID%' OR org_name = 'Rival Events')
     + (SELECT COUNT(*) FROM Tournament   WHERE name LIKE 'ACID%')
     + (SELECT COUNT(*) FROM Transactions WHERE category = 'acid probe')
     + (SELECT COUNT(*) FROM Payments     WHERE tournament_id = 2 AND staff_user_id = 12 AND amount = 750.00)
     + (SELECT COUNT(*) FROM Registration WHERE competitor_id = 5 AND tournament_id = 2)
  INTO @residue
--------------

--------------
INSERT INTO acid_results (test, property, verdict, evidence, detail) VALUES
('RESIDUE', 'P', IF(@residue = 0, 'OK', 'FAIL'),
 CONCAT('stray rows left by this file: ', @residue),
 'the schema and data must be unchanged after the run')
--------------

--------------
-- =====================================================================
--------------

--------------
-- VERDICT TABLE -- read this first.
--------------

--------------
-- =====================================================================
--------------

--------------
SELECT seq, test, property, verdict, evidence, detail
FROM acid_results ORDER BY seq
--------------

+-----+-----------+----------+---------+---------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------------------------------------+
| seq | test      | property | verdict | evidence                                                                  | detail                                                                                                                                                      |
+-----+-----------+----------+---------+---------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------------------------------------+
|   1 | P0-engine | P        | OK      | non-InnoDB tables: 0                                                      | InnoDB is required for START TRANSACTION, ROLLBACK, row locks and FKs to be real                                                                            |
|   2 | P0-check  | P        | OK      | 19 CHECK constraints declared; negative amount accepted: 0                | CHECK constraints must be enforced, not merely parsed                                                                                                       |
|   3 | A1        | A        | GAP     | orphan payments left behind: 1                                            | payout committed with no matching expense; the two statements are not atomic                                                                                |
|   4 | A1-FIX    | A        | OK      | residue after ROLLBACK: 0                                                 | wrapping the pair in a transaction makes the payout all-or-nothing                                                                                          |
|   5 | A2        | A        | GAP     | competitors registered for nothing: 1                                     | a Players and Competitor row survived a failed Registration insert                                                                                          |
|   6 | A5        | A        | GAP     | org rows surviving ROLLBACK: 1                                            | CREATE USER is DDL and forces an implicit commit; Sprint 3 account provisioning is not atomic with its data                                                 |
|   7 | A6        | A        | GAP     | matches left: 0, tournament rows left: 1                                  | bracket destroyed while the tournament survived; a hand-rolled cascade is not atomic even after 10_acid_fixes.sql, because each DELETE is its own statement |
|   8 | A6-FIX    | A        | OK      | matches intact: 3 of 3, participants intact: 6 of 6                       | one statement is atomic on its own; the failed parent delete took its cascades down with it                                                                 |
|   9 | C1        | C        | GAP     | cross-game registrations accepted: 1                                      | a Valorant team was entered into the TFT event; v_registration_violations only detects this after commit                                                    |
|  10 | C1-FIX    | C        | OK      | rows created by the procedure: 0                                          | sp_register_team compares Teams.game_id against Tournament.game_id and refuses                                                                              |
|  11 | C1-HAPPY  | C        | OK      | valid registrations accepted: 1                                           | the fix must not reject legal work                                                                                                                          |
|  12 | C2        | C        | GAP     | unregistered participants accepted: 1                                     | a solo TFT player was placed in a Valorant final                                                                                                            |
|  13 | C2-FIX    | C        | OK      | rows created by the procedure: 0                                          | sp_record_placement requires a matching Registration row                                                                                                    |
|  14 | C3        | C        | GAP     | competitors at placement 1 in match 7: 2                                  | two first places in one match; add UNIQUE (match_id, placement)                                                                                             |
|  15 | C3-FIX    | C        | OK      | competitors at placement 1 in match 7: 1                                  | sp_record_placement rejects a placement already taken in that match                                                                                         |
|  16 | C4        | C        | GAP     | competitors on seed 1 in tournament 2: 2                                  | duplicate seed accepted; add UNIQUE (tournament_id, seed)                                                                                                   |
|  17 | C4-FIX    | C        | OK      | competitors on seed 1 in tournament 2: 1                                  | sp_register_team refuses a seed that is already taken                                                                                                       |
|  18 | C5        | C        | GAP     | active roster rows for player 1: 2                                        | player active on two teams simultaneously; needs a trigger, no CHECK can span rows                                                                          |
|  19 | C6        | C        | GAP     | matches outside their tournament window: 1                                | match accepted 20 months after the tournament ended                                                                                                         |
|  20 | C7        | C        | GAP     | paid staff who are not org members: 1                                     | a player captain was accepted as paid tournament staff                                                                                                      |
|  21 | C8        | C        | GAP     | at 2026-03-16 15:00 -- caster slots: 2, creator slots: 2, player slots: 2 | caster, creator and player all double-booked at the same instant                                                                                            |
|  22 | C9        | C        | GAP     | pool 5000.00 vs team payouts 7500.00                                      | total payouts exceeded the prize pool with no constraint objecting                                                                                          |
|  23 | C9-FIX    | C        | OK      | overdrawing payments created: 0                                           | sp_pay_team_prize checks headroom under FOR UPDATE and refuses                                                                                              |
|  24 | C10       | C        | GAP     | payments to unregistered teams: 1                                         | prize money owed to a team that never entered that tournament                                                                                               |
|  25 | C10-FIX   | C        | OK      | payments created: 0                                                       | sp_pay_team_prize requires the team to be registered                                                                                                        |
|  26 | C11       | C        | GAP     | mismatched transactions: 1, mismatched contracts: 1                       | org_id need not match the tournament owner, so v_org_financials can show another org rows                                                                   |
|  27 | C12       | C        | GAP     | NULL end_date tournaments: 1, duplicate NULL jerseys on team 3: 2         | NULL evaluates a CHECK to UNKNOWN (accepted) and does not collide in a UNIQUE                                                                               |
|  28 | C13       | C        | GAP     | booked 1200.00 vs paid 800.00 vs agreed 800.00                            | already inconsistent in committed data -- nothing was injected for this test                                                                                |
|  29 | RESIDUE   | P        | OK      | stray rows left by this file: 0                                           | the schema and data must be unchanged after the run                                                                                                         |
+-----+-----------+----------+---------+---------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------------------------------------+
--------------
SELECT verdict, COUNT(*) AS n FROM acid_results GROUP BY verdict ORDER BY verdict
--------------

+---------+----+
| verdict | n  |
+---------+----+
| GAP     | 17 |
| OK      | 12 |
+---------+----+
--------------
SELECT COUNT(*) AS unfixed_gaps
     , GROUP_CONCAT(test ORDER BY seq SEPARATOR ', ') AS gap_list
FROM acid_results WHERE verdict = 'GAP'
--------------

+--------------+------------------------------------------------------------------------+
| unfixed_gaps | gap_list                                                               |
+--------------+------------------------------------------------------------------------+
|           17 | A1, A2, A5, A6, C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12, C13 |
+--------------+------------------------------------------------------------------------+

=====================================================================
 SUMMARY
=====================================================================
mysql: [Warning] Using a password on the command line interface can be insecure.
+---------+----+
| verdict | n  |
+---------+----+
| GAP     | 17 |
| OK      | 12 |
+---------+----+
+------+----------+---------+---------------------------------------------------------------------------+
| test | property | verdict | evidence                                                                  |
+------+----------+---------+---------------------------------------------------------------------------+
| A1   | A        | GAP     | orphan payments left behind: 1                                            |
| A2   | A        | GAP     | competitors registered for nothing: 1                                     |
| A5   | A        | GAP     | org rows surviving ROLLBACK: 1                                            |
| A6   | A        | GAP     | matches left: 0, tournament rows left: 1                                  |
| C1   | C        | GAP     | cross-game registrations accepted: 1                                      |
| C2   | C        | GAP     | unregistered participants accepted: 1                                     |
| C3   | C        | GAP     | competitors at placement 1 in match 7: 2                                  |
| C4   | C        | GAP     | competitors on seed 1 in tournament 2: 2                                  |
| C5   | C        | GAP     | active roster rows for player 1: 2                                        |
| C6   | C        | GAP     | matches outside their tournament window: 1                                |
| C7   | C        | GAP     | paid staff who are not org members: 1                                     |
| C8   | C        | GAP     | at 2026-03-16 15:00 -- caster slots: 2, creator slots: 2, player slots: 2 |
| C9   | C        | GAP     | pool 5000.00 vs team payouts 7500.00                                      |
| C10  | C        | GAP     | payments to unregistered teams: 1                                         |
| C11  | C        | GAP     | mismatched transactions: 1, mismatched contracts: 1                       |
| C12  | C        | GAP     | NULL end_date tournaments: 1, duplicate NULL jerseys on team 3: 2         |
| C13  | C        | GAP     | booked 1200.00 vs paid 800.00 vs agreed 800.00                            |
+------+----------+---------+---------------------------------------------------------------------------+
